server {
    listen 80;
    server_name localhost;

    location / {
        root /var/www/html;  # Path to your Vaadin app's build files
        index index.html;    # Default HTML file to serve
    }
}